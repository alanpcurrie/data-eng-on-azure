commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit $1

pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,json,md}"
      run: pnpm biome check --apply {staged_files}
